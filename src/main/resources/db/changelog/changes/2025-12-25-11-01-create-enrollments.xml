<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="2025-12-25-10-05-00-create-enrollments" author="mergen">

        <createTable tableName="enrollments">
            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true"/>
            </column>

            <column name="course_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <column name="student_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <column name="enrolled_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addForeignKeyConstraint
                baseTableName="enrollments" baseColumnNames="course_id"
                referencedTableName="courses" referencedColumnNames="id"
                constraintName="fk_enrollments_course"/>

        <addForeignKeyConstraint
                baseTableName="enrollments" baseColumnNames="student_id"
                referencedTableName="users" referencedColumnNames="id"
                constraintName="fk_enrollments_student"/>

        <!-- запрет двойной записи одного студента на один курс -->
        <addUniqueConstraint tableName="enrollments"
                             columnNames="course_id, student_id"
                             constraintName="uq_course_student"/>

    </changeSet>

</databaseChangeLog>
